/**
 * Placeholder database types.
 *
 * Full generated types come from `npm run db:types` (Supabase Management API,
 * needs SUPABASE_ACCESS_TOKEN). Until then the Supabase clients are loosely
 * typed via this `any` alias — swapping in the generated file is a no-op for
 * every caller. Do not hand-edit beyond this; it will be overwritten.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
