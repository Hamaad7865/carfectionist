import { Settings2 } from "lucide-react";
import { PagePlaceholder } from "@/components/shell/PagePlaceholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      icon={Settings2}
      title="Team & Settings"
      phase="Phase 1 & 3"
      description="Business profile, the Diamondbrite document template editor (banners, terms, section toggles, fiscal-locked fields), staff and roles, and receipt configuration for the tablet."
    />
  );
}
